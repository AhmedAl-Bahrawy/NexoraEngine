# Constants Reference

## DATABASE

```typescript
DATABASE.DEFAULT_SCHEMA = 'public'
DATABASE.PAGINATION.DEFAULT_PAGE = 1
DATABASE.PAGINATION.DEFAULT_PAGE_SIZE = 20
DATABASE.PAGINATION.MAX_PAGE_SIZE = 100
DATABASE.DEFAULT_TIMEOUT = 10000        // 10 seconds
DATABASE.DEFAULT_RETRIES = 2
DATABASE.RETRY_DELAY = 1000             // 1 second
DATABASE.MAX_QUERY_PARAMS = 50
```

## CACHE

```typescript
CACHE.DEFAULT_TTL = 300000              // 5 minutes
CACHE.MAX_SIZE = 100
CACHE.CLEANUP_INTERVAL = 60000          // 1 minute
CACHE.KEY_SEPARATOR = ':'
```

## AUTH

```typescript
AUTH.SESSION_KEY = 'sb-auth-token'
AUTH.PASSWORD_MIN_LENGTH = 6
AUTH.DEFAULT_REDIRECT = '/'
```

## REALTIME

```typescript
REALTIME.SUBSCRIBE_STATES.SUBSCRIBED = 'SUBSCRIBED'
REALTIME.SUBSCRIBE_STATES.CHANNEL_ERROR = 'CHANNEL_ERROR'
REALTIME.SUBSCRIBE_STATES.TIMED_OUT = 'TIMED_OUT'
REALTIME.SUBSCRIBE_STATES.CLOSED = 'CLOSED'
REALTIME.EVENTS.INSERT = 'INSERT'
REALTIME.EVENTS.UPDATE = 'UPDATE'
REALTIME.EVENTS.DELETE = 'DELETE'
REALTIME.EVENTS.ALL = '*'
REALTIME.DEFAULT_TIMEOUT = 30000
```

## STORAGE

```typescript
STORAGE.MAX_FILE_SIZE = 52428800        // 50MB
STORAGE.ALLOWED_TYPES = ['image/jpeg', 'image/png', 'image/gif', 'application/pdf']
STORAGE.UPLOAD_CHUNK_SIZE = 6291456     // 6MB
```

## ERRORS

```typescript
ERRORS.NETWORK = 'network_error'
ERRORS.DATABASE = 'database_error'
ERRORS.AUTH = 'auth_error'
ERRORS.VALIDATION = 'validation_error'
ERRORS.NOT_FOUND = 'not_found'
ERRORS.UNAUTHORIZED = 'unauthorized'
ERRORS.FORBIDDEN = 'forbidden'
ERRORS.RATE_LIMIT = 'rate_limit'
ERRORS.TIMEOUT = 'timeout_error'
ERRORS.CONFIG = 'config_error'
```

## ENV_KEYS

```typescript
ENV_KEYS.SUPABASE_URL = 'VITE_SUPABASE_URL'
ENV_KEYS.SUPABASE_ANON_KEY = 'VITE_SUPABASE_ANON_KEY'
ENV_KEYS.SUPABASE_SERVICE_KEY = 'SUPABASE_SERVICE_ROLE_KEY'
```
